{
  "simulation": {
    "totalRounds": 60,
    "initialRounds": 25,
    "description": "25 velas iniciais (já na tela), 35 velas jogáveis"
  },
  "generation": {
    "houseEdge": 0.04,
    "description": "House edge de 4% (realista)"
  },
  "bankroll": {
    "initial": 1000.00,
    "bet2x": 100.00,
    "bet10x": 50.00
  },
  "rules": {
    "pinkStrategy": {
      "minConfidence": 75,
      "minInterval": 5,
      "minOccurrencesByInterval": {
        "short": { "max": 4, "min": 4 },
        "medium": { "max": 9, "min": 3 },
        "long": { "max": 999, "min": 2 }
      },
      "description": "Confiança 75%, intervalo ≥5, ocorrências por faixa"
    },
    "purpleStrategy": {
      "minConversion": 60,
      "minStreakToValidate": 2,
      "minStreakToPlay": 3,
      "description": "Conversão 60%, exige 2 roxas para validar, 3 para jogar"
    },
    "stopLoss": {
      "consecutiveReds": 2,
      "description": "Para após 2 reds seguidos"
    },
    "postPinkLock": {
      "candlesToWait": 3,
      "doubleBlueException": true,
      "description": "Aguarda 3 velas após rosa (exceção: double blue ≤1)"
    },
    "deepDowntrend": {
      "consecutiveBlues": 3,
      "recoveryPurples": 3,
      "description": "3 azuis seguidas = exige 3 roxas para recuperar"
    }
  }
}
